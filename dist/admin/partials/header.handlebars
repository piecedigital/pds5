<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
        <title>{{title}}</title>
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" type="text/css" media="screen" href="/public/css/style.css" />
    </head>
    <body>
        <div class="react-app">
            <div class="admin-dashboard">
                <div class="nav">
                    <div class="item">
                        <a href="/">View Site</a>
                    </div>
                    <div class="item">
                        <a href="/pc_admin/logout">Logout </a>
                    </div>
                </div>
                <section class="side-panel">
                    <a href="/pc_admin" class="row logo">
                        <h4>Piece Digital CMS</h4>
                    </a>
                    {{#each adminViews}}
                        <a key={{directory}}-{{ind}} href="/pc_admin/plugin/{{slug}}" class="row">
                            <h4>{{name}}</h4>
                        </a>
                    {{/each}}
                </section>
                <section class="main-content">